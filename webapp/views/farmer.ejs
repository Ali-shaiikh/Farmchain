<!DOCTYPE html>
<html lang="en" id="html">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title data-en="Farmer Portal" data-mr="शेतकरी पोर्टल">Farmer Portal</title>

  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Noto+Serif+Devanagari:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/speech.css">

  <style>
    body { 
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #f0fdf4 0%, #dbeafe 100%);
    }
    [lang="mr"] { font-family: 'Noto Serif Devanagari', sans-serif; }

    .modal {
      display: none;
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.6);
      z-index: 50;
      backdrop-filter: blur(4px);
    }

    .modal-content {
      background: #fff;
      margin: 10% auto;
      padding: 32px;
      width: 90%;
      max-width: 600px;
      border-radius: 20px;
      box-shadow: 0 25px 50px rgba(0,0,0,0.25);
      animation: slideIn 0.3s ease-out;
    }

    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(-30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .card-hover {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .card-hover:hover {
      transform: translateY(-8px);
    }

    .gradient-text {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .section-divider {
      position: relative;
      margin: 4rem 0;
    }

    .section-divider::before {
      content: '';
      position: absolute;
      left: 50%;
      transform: translateX(-50%);
      width: 100px;
      height: 4px;
      background: linear-gradient(90deg, transparent, #10b981, transparent);
      border-radius: 2px;
    }

    .stat-card {
      background: linear-gradient(135deg, #ffffff 0%, #f0fdf4 100%);
      border-left: 4px solid #10b981;
    }

    .filter-section {
      background: linear-gradient(135deg, #ffffff 0%, #fefce8 100%);
      border: 2px solid #fbbf24;
    }

    .equipment-card {
      background: white;
      border: 1px solid #e5e7eb;
      position: relative;
      overflow: hidden;
    }

    .equipment-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 4px;
      background: linear-gradient(90deg, #10b981, #059669);
      transform: scaleX(0);
      transition: transform 0.3s ease;
    }

    .equipment-card:hover::before {
      transform: scaleX(1);
    }

    .badge {
      display: inline-block;
      padding: 4px 12px;
      border-radius: 12px;
      font-size: 0.75rem;
      font-weight: 600;
    }

    .lang-btn {
      padding: 8px 20px;
      border-radius: 10px;
      font-weight: 600;
      transition: all 0.2s;
      border: 2px solid transparent;
    }

    .lang-btn.active {
      background: #10b981;
      color: white;
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.4);
    }

    .lang-btn:not(.active) {
      background: white;
      border-color: #e5e7eb;
      color: #64748b;
    }

    .lang-btn:not(.active):hover {
      border-color: #10b981;
      color: #10b981;
    }

    .hero-section {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      border-radius: 24px;
      padding: 48px;
      color: white;
      box-shadow: 0 20px 40px rgba(16, 185, 129, 0.3);
      position: relative;
      overflow: hidden;
    }

    .hero-section::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -10%;
      width: 400px;
      height: 400px;
      background: rgba(255, 255, 255, 0.1);
      border-radius: 50%;
    }

    .section-header {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 32px;
    }

    .section-icon {
      width: 48px;
      height: 48px;
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      border-radius: 12px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 24px;
    }

    .order-card {
      background: linear-gradient(135deg, #ffffff 0%, #eff6ff 100%);
      border-left: 4px solid #3b82f6;
    }

    .price-tag {
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      color: white;
      padding: 8px 16px;
      border-radius: 12px;
      font-weight: 700;
      font-size: 1.125rem;
      display: inline-block;
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
    }
  </style>
</head>

<body class="min-h-screen text-slate-800">

<!-- Speech Controls -->
<div id="speech-controls" class="speech-controls">
  <div class="text-sm font-semibold mb-2">Text-to-Speech</div>
  <button id="stop-speech" class="speech-btn">Stop</button>
  <button id="pause-speech" class="speech-btn">Pause</button>
  <button id="resume-speech" class="speech-btn">Resume</button>
</div>

<%- include('partials/header') %>

<div class="max-w-7xl mx-auto px-6 py-12">

  <!-- Language Switch - Floating -->
  <div class="flex justify-end gap-3 mb-8">
    <button class="lang-btn active" onclick="window.FarmChainSpeech.setLanguage('en')">🇬🇧 English</button>
    <button class="lang-btn" onclick="window.FarmChainSpeech.setLanguage('mr')">🇮🇳 मराठी</button>
  </div>

  <!-- Hero Welcome Section -->
  <div class="hero-section mb-12 relative z-10">
    <div class="relative z-10">
      <h1 class="text-4xl md:text-5xl font-bold mb-4 speakable">
        Welcome back, <%= farmerName %>! 👋
      </h1>
      <p class="text-emerald-50 text-lg mb-8 max-w-2xl">
        Discover and rent the best farm equipment in your region. Manage your bookings, track usage analytics, and grow your farming business.
      </p>
      <a href="/farmer/analytics"
         class="inline-block bg-white text-emerald-700 px-8 py-4 rounded-xl shadow-xl hover:shadow-2xl transition font-bold text-lg hover:scale-105 transform">
        📊 View Usage Analytics →
      </a>
    </div>
  </div>

  <!-- Quick Stats Section -->
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
    <div class="stat-card rounded-2xl shadow-lg p-6 card-hover">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-slate-500 text-sm font-medium mb-1">Available Equipment</p>
          <p class="text-3xl font-bold text-emerald-700"><%= listings ? listings.length : 0 %></p>
        </div>
        <div class="text-5xl">🚜</div>
      </div>
    </div>
    
    <div class="stat-card rounded-2xl shadow-lg p-6 card-hover">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-slate-500 text-sm font-medium mb-1">Active Bookings</p>
          <p class="text-3xl font-bold text-blue-700"><%= bookings ? bookings.filter(b => b.status === 'confirmed').length : 0 %></p>
        </div>
        <div class="text-5xl">📦</div>
      </div>
    </div>
    
    <div class="stat-card rounded-2xl shadow-lg p-6 card-hover">
      <div class="flex items-center justify-between">
        <div>
          <p class="text-slate-500 text-sm font-medium mb-1">Regions Available</p>
          <p class="text-3xl font-bold text-purple-700"><%= REGIONS ? REGIONS.length : 0 %></p>
        </div>
        <div class="text-5xl">📍</div>
      </div>
    </div>
  </div>

  <!-- Filters Section -->
  <div class="filter-section rounded-3xl shadow-xl p-8 mb-16">
    <div class="section-header">
      <div class="section-icon">🔍</div>
      <h2 class="text-2xl font-bold text-slate-800">Find Equipment</h2>
    </div>
    
    <form class="grid grid-cols-1 md:grid-cols-4 gap-4 items-end" method="GET">
      <div>
        <label class="block text-sm font-semibold text-slate-700 mb-2">📍 Region</label>
        <select name="region" class="w-full border-2 border-slate-200 rounded-xl px-4 py-3 focus:border-emerald-500 focus:outline-none transition">
          <option value="">All Regions</option>
          <% REGIONS.forEach(function(r) { %>
            <option value="<%= r %>" <%= region === r ? 'selected' : '' %>><%= r %></option>
          <% }); %>
        </select>
      </div>

      <div>
        <label class="block text-sm font-semibold text-slate-700 mb-2">🏷️ Category</label>
        <select name="category" class="w-full border-2 border-slate-200 rounded-xl px-4 py-3 focus:border-emerald-500 focus:outline-none transition">
          <option value="all">All Categories</option>
          <% CATEGORIES.forEach(function(c) { %>
            <option value="<%= c %>" <%= category === c ? 'selected' : '' %>><%= CATEGORY_TRANSLATIONS[c] || c %></option>
          <% }); %>
        </select>
      </div>

      <div>
        <label class="block text-sm font-semibold text-slate-700 mb-2">🔎 Search</label>
        <input type="text"
               name="q"
               placeholder="Search equipment..."
               value="<%= query %>"
               class="w-full border-2 border-slate-200 rounded-xl px-4 py-3 focus:border-emerald-500 focus:outline-none transition"/>
      </div>

      <button class="bg-emerald-600 hover:bg-emerald-700 text-white px-8 py-3 rounded-xl transition font-bold shadow-lg hover:shadow-xl transform hover:scale-105">
        Apply Filters
      </button>
    </form>
  </div>

  <!-- Equipment Listings Section -->
  <div class="section-divider mb-12"></div>
  
  <div class="section-header">
    <div class="section-icon">🌾</div>
    <div>
      <h2 class="text-3xl font-bold gradient-text speakable">Available Equipment</h2>
      <p class="text-slate-500 text-sm mt-1">Rent quality equipment from trusted sellers</p>
    </div>
  </div>

  <% if(listings && listings.length > 0) { %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <% listings.forEach(function(l) { 
        const imgUrl = (l.img && l.img.trim() !== "") ? l.img : "https://via.placeholder.com/400x300/10b981/ffffff?text=Farm+Equipment";
      %>
        <div class="equipment-card rounded-2xl shadow-lg hover:shadow-2xl card-hover flex flex-col">
          <div class="h-52 bg-cover bg-center relative" style="background-image:url('<%= imgUrl %>')">
            <div class="absolute top-4 right-4">
              <span class="badge bg-emerald-100 text-emerald-700 shadow-md">
                <%= CATEGORY_TRANSLATIONS[l.category] || l.category %>
              </span>
            </div>
          </div>

          <div class="p-6 flex flex-col flex-1">
            <h3 class="font-bold text-xl mb-3 text-slate-800 speakable">
              <%= CATEGORY_TRANSLATIONS[l.name] || l.name %>
            </h3>

            <div class="flex items-center gap-2 mb-3 text-sm text-slate-600">
              <span class="badge bg-slate-100 text-slate-700">📍 <%= l.region %></span>
              <span class="badge bg-blue-50 text-blue-700">✨ <%= l.condition %></span>
            </div>

            <div class="mb-6">
              <span class="price-tag">₹<%= l.pricePerDay %><span class="text-sm font-normal opacity-90"> / day</span></span>
            </div>

            <form method="POST" action="/farmer/initiate-booking/<%= l._id %>" class="mt-auto">
              <div class="flex gap-3">
                <div class="flex-1">
                  <input type="number"
                         name="days"
                         min="1"
                         required
                         placeholder="Days"
                         class="w-full border-2 border-slate-200 rounded-xl px-4 py-3 focus:border-emerald-500 focus:outline-none transition font-semibold"/>
                </div>
                <button class="bg-gradient-to-r from-emerald-600 to-emerald-700 hover:from-emerald-700 hover:to-emerald-800 text-white px-6 py-3 rounded-xl transition font-bold shadow-lg hover:shadow-xl flex-shrink-0">
                  Book Now
                </button>
              </div>
            </form>
          </div>
        </div>
      <% }); %>
    </div>
  <% } else { %>
    <div class="text-center py-20 bg-white rounded-3xl shadow-lg">
      <div class="text-6xl mb-4">🔍</div>
      <p class="text-xl text-slate-500 font-medium">No equipment found</p>
      <p class="text-slate-400 mt-2">Try adjusting your filters</p>
    </div>
  <% } %>

  <!-- Orders Section -->
  <div class="section-divider mt-20 mb-12"></div>
  
  <div class="section-header">
    <div class="section-icon" style="background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);">📋</div>
    <div>
      <h2 class="text-3xl font-bold text-slate-800 speakable">Your Active Orders</h2>
      <p class="text-slate-500 text-sm mt-1">Track and manage your equipment bookings</p>
    </div>
  </div>

  <% if (bookings && bookings.length > 0) { %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      <% bookings.forEach(function(booking) { if (booking.status === 'confirmed') { %>
        <div class="order-card rounded-2xl shadow-lg p-6 hover:shadow-2xl card-hover">
          <div class="flex items-start justify-between mb-4">
            <div>
              <h3 class="font-bold text-lg text-slate-800 mb-1"><%= booking.listing.name %></h3>
              <p class="text-sm text-slate-500">👤 <%= booking.listing.sellerName %></p>
            </div>
            <span class="badge bg-green-100 text-green-700">Active</span>
          </div>

          <div class="space-y-3 mb-6">
            <div class="flex justify-between items-center">
              <span class="text-slate-600 text-sm">Duration:</span>
              <span class="font-semibold text-slate-800"><%= Math.round((booking.to - booking.from) / 86400000) %> days</span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-slate-600 text-sm">Total Amount:</span>
              <span class="font-bold text-emerald-700 text-lg">₹<%= booking.amount %></span>
            </div>
          </div>

          <button onclick="showOrderDetails('<%= JSON.stringify(booking).replace(/'/g, "\\'") %>')"
                  class="w-full bg-gradient-to-r from-blue-600 to-blue-700 hover:from-blue-700 hover:to-blue-800 text-white px-6 py-3 rounded-xl transition font-bold shadow-lg hover:shadow-xl">
            View Full Details →
          </button>
        </div>
      <% }}); %>
    </div>
  <% } else { %>
    <div class="text-center py-20 bg-white rounded-3xl shadow-lg">
      <div class="text-6xl mb-4">📭</div>
      <p class="text-xl text-slate-500 font-medium">No active orders</p>
      <p class="text-slate-400 mt-2">Start booking equipment to see them here</p>
    </div>
  <% } %>

</div>

<!-- Order Details Modal -->
<div id="orderModal" class="modal">
  <div class="modal-content">
    <div class="flex items-center justify-between mb-6">
      <h2 class="text-2xl font-bold text-slate-800">📋 Order Details</h2>
      <button class="text-3xl text-slate-400 hover:text-slate-600 transition font-bold w-10 h-10 flex items-center justify-center"
              onclick="document.getElementById('orderModal').style.display='none'">×</button>
    </div>
    <div id="orderDetails" class="text-slate-700 space-y-3"></div>
  </div>
</div>

<%- include('partials/footer') %>

<script src="/js/speech.js"></script>
<script>
  document.addEventListener('DOMContentLoaded', () => {
    window.FarmChainSpeech.setLanguage('en');
  });

  function showOrderDetails(bookingJson) {
    const booking = JSON.parse(bookingJson);
    const days = Math.round((new Date(booking.to) - new Date(booking.from)) / 86400000);
    
    document.getElementById('orderDetails').innerHTML = `
      <div class="bg-slate-50 rounded-xl p-4 space-y-3">
        <div class="flex justify-between py-2 border-b border-slate-200">
          <span class="font-semibold text-slate-600">Booking ID:</span>
          <span class="text-slate-800 font-mono text-sm">${booking._id}</span>
        </div>
        <div class="flex justify-between py-2 border-b border-slate-200">
          <span class="font-semibold text-slate-600">Equipment:</span>
          <span class="text-slate-800 font-bold">${booking.listing.name}</span>
        </div>
        <div class="flex justify-between py-2 border-b border-slate-200">
          <span class="font-semibold text-slate-600">Seller:</span>
          <span class="text-slate-800">${booking.listing.sellerName}</span>
        </div>
        <div class="flex justify-between py-2 border-b border-slate-200">
          <span class="font-semibold text-slate-600">Duration:</span>
          <span class="text-slate-800 font-bold">${days} days</span>
        </div>
        <div class="flex justify-between py-2 border-b border-slate-200">
          <span class="font-semibold text-slate-600">Total Amount:</span>
          <span class="text-emerald-700 font-bold text-xl">₹${booking.amount}</span>
        </div>
        <div class="flex justify-between py-2 border-b border-slate-200">
          <span class="font-semibold text-slate-600">Transaction Hash:</span>
          <span class="text-slate-800 font-mono text-xs break-all">${booking.txHash}</span>
        </div>
        <div class="flex justify-between py-2">
          <span class="font-semibold text-slate-600">Booking Date:</span>
          <span class="text-slate-800">${new Date(booking.createdAt).toLocaleString()}</span>
        </div>
      </div>
    `;
    document.getElementById('orderModal').style.display = "block";
  }

  // Close modal on outside click
  window.onclick = function(event) {
    const modal = document.getElementById('orderModal');
    if (event.target == modal) {
      modal.style.display = "none";
    }
  }
</script>

</body>
</html>